# P1.2 worker 直读 Capture()，删除 ChainContext 类型

**工时**：1 周  **owner**：TBD  **前置**：P1.1  **阻塞**：P1.3 / P3.*

## 1. 目标

废除 v2.3 引入的 `ChainContext` 类型概念；worker 直接持 `Chainstate::Snapshot` 局部变量。删除 ChainContextRef / ChainContextPtr 等所有相关 typedef 和文件。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/validation/chain_context.h | 删除（如已存在）|
| src/validation/chain_context.cpp | 删除 |
| 全代码库 | grep `ChainContext` 调用点替换 |
| src/test/chain_context_tests.cpp | 删除 / 重写 |

## 3. 代码改造

```cpp
// 旧（v2.3，本卡删除）：
// ChainContextRef ctx = GetCurrentChainContext();
// uint32_t flags = ctx->script_flags;

// 新（v2.6.1）：
auto snap = GetChainstate().Capture();
uint32_t flags = snap.script_flags;
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| `ChainContext` 在代码库 0 引用 | grep + AST |
| 老编译通过 | CI |
| 单元 + functional 全过 | 100% |

## 5. 测试用例

无新功能；现有测试覆盖。

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R3 worker hot path snap 一致性 | 局部变量，验证完丢弃 |

## 7. 回滚条件

老测试失败 → revert，重新审视 ChainContext 是否必要。

## 8. 审核 checklist

- [ ] 0 ChainContext 引用
- [ ] 删除文件确认无外部依赖
- [ ] code review 1 架构

## 9. 依赖 / 阻塞

- **前置**：P1.1
- **阻塞**：P1.3 / P3.*
