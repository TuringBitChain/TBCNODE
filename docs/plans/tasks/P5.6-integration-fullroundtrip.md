# P5.6 集成测试 + P3 末 full round-trip

**工时**：1 周  **owner**：TBD  **前置**：P5.1-P5.5 P0.0b.4  **阻塞**：M2

## 1. 目标

P5 全部改动合入后跑 full round-trip（F5 P3 末）：

- vN（v2.6.1 完整版）跑 1000 regtest 块（含 dispatcher / libcuckoo / seqlock / worker / signal_dispatcher / RaceStash）
- 关停 → v1 binary 启动同 datadir 必须无 reindex 立即同步
- 反向：v1 跑 1000 块 → vN 启动同样无 reindex

这是 v2.6.1 全部改动后的真兼容性 KPI（不再是 P0.0b smoke level）。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| test/functional/binary_roundtrip_full.py | 新增（基于 P0.0b.4 smoke 扩展）|
| docs/plans/spike-results/P5.6-fullroundtrip-report.md | 报告 |

## 3. 代码改造

```python
# test/functional/binary_roundtrip_full.py
class FullRoundtripTest(BitcoinTestFramework):
    def run_test(self):
        # vN 跑 1000 块（含 dispatcher / 真完整改造路径）
        # 关停 → v1 启动 → 验证启动 < 30s + tip 一致
        # vN 又启动 → 验证又能继续

        # 还要测：
        # - vN 关停时如果 RaceStash / ReorgStash 非空，关停 + 重启后 stash 怎么处理
        # - vN 关停时 in-flight worker 跑一半，关停 + 重启后 inflight 状态恢复
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| vN → v1 → vN round-trip 启动 < 30s | 双向通过 |
| chain tip + UTXO hash 跨切换 100% 一致 | 双向通过 |
| stash 非空关停 → 重启后 0 数据丢失 | 集成 |
| in-flight 关停 + 重启 0 race / 0 数据损坏 | ASan |
| 4 周 shadow node baseline 准备就绪 | P6 阶段输入 |

## 5. 测试用例

```bash
V1_BINARY=$HOME/build-v1/src/bitcoind \
VN_BINARY=$PWD/build/src/bitcoind \
test/functional/binary_roundtrip_full.py
```

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R7 关停 + 重启数据损坏 | 双向 round-trip + ASan |
| R5 stash 非空关停丢 tx | 关停时持久化（mempool.dat 已有）|

## 7. 回滚条件

测试红 → 排查具体 P5 卡 → revert。

## 8. 审核 checklist

- [ ] 双向 round-trip 通过
- [ ] stash 关停 + 重启 0 丢失
- [ ] code review 1 架构 + 1 cpp

## 9. 依赖 / 阻塞

- **前置**：P5.1-P5.5 P0.0b.4
- **阻塞**：M2
