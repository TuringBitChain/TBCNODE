# P1.1 Chainstate seqlock 完整实现（K1 fence + tip_index + genesisActivationHeight + F2 守卫）

**工时**：1 周  **owner**：TBD  **前置**：M0 + P0.0b.1 + P0.0a.5  **阻塞**：P1.2-P1.7 / P3.* / P4.1

## 1. 目标

实现 `Chainstate` 类（seqlock 元数据存储），含 7 字段（tip_hash + tip_index + script_flags + height + mtp + isGenesisEnabled + genesisActivationHeight）；F2 单写者 RAII try_lock 守卫；K1 显式 fence。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/validation/chainstate.h | 新增 |
| src/validation/chainstate.cpp | 新增 |
| src/validation/CMakeLists.txt | 加入 |
| src/test/chainstate_seqlock_tests.cpp | 新增 |

## 3. 代码改造

完整代码见 detailed-design §1.1（v2.6.1 修订），含 C-A tip_index + H-D genesisActivationHeight + H-F RAII probe + K1 release/acquire fence + memcpy 32 字节字段。

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| 单 writer × 32 reader 长压 0 torn | 10 分钟 + cross-arch（x86/ARM/RISC-V） |
| F2 守卫跨线程调用直接 abort | death test 通过 |
| F2 守卫同线程已持锁正常 | unit 通过 |
| K1 fence 在 ARM/RISC-V 显式 | grep 验证 atomic_thread_fence |
| TSan 72h | 0 race |

## 5. 测试用例

```cpp
TEST(ChainstateSeqlock, NoTornReadUnderLoad10min) { /* 见 P0.0b.1 */ }
TEST(ChainstateSeqlock, F2GuardSelfThreadOK) { /* H-F */ }
TEST(ChainstateSeqlock, F2GuardCrossThreadAborts) { /* death test */ }
TEST(ChainstateSeqlock, F2GuardExceptionRAIIBalanced) { /* H-F 异常注入 */ }
TEST(ChainstateSeqlock, AllSevenFieldsCaptured) { /* C-A + H-D */ }
```

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R2 跨架构 fence | P0.0b.1 已验证 + 本卡 cross-arch 测试 |
| R7 守卫异常路径 | RAII unique_lock + ASan 测试 |

## 7. 回滚条件

torn 出现 / F2 守卫失效 → revert seqlock，回到 v2.5 chainstate.cs shared_mutex 设计；触发 M1 警告。

## 8. 审核 checklist

- [ ] 7 字段全覆盖
- [ ] F2 守卫 5 项语义全过
- [ ] cross-arch 0 torn
- [ ] code review 1 cpp + 1 安全 + 1 架构

## 9. 依赖 / 阻塞

- **前置**：M0 + P0.0b.1 + P0.0a.5
- **阻塞**：P1.2-P1.7 / P3.* / P4.1
