# P4.3 BatchWrite 阻塞时长 benchmark + 分片优化预研

**工时**：1-2 周  **owner**：TBD  **前置**：P4.1  **阻塞**：M2

## 1. 目标

BatchWrite 真主网量级（10 万 UTXO/块）阻塞时长 benchmark；如果 p99 > 200ms 触发分片优化预研（按 outpoint hash sharding）。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/bench/batchwrite_real_workload_bench.cpp | 新增 |
| docs/plans/spike-results/P4.3-batchwrite-shard-research.md | 预研报告 |

## 3. 代码改造

```cpp
static void BatchWrite_RealWorkload(benchmark::State& state) {
    // 真实 mainnet 区块导入序，10 万 UTXO/块 × 100 块
    // 测 unique_lock(batchWriteMtx) 持锁时长 + worker 排队等待时长
}
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| 10 万 UTXO BatchWrite p99 | ≤ 200ms |
| 含 32 worker 排队 BatchWrite p99 | ≤ 300ms |
| 100 块连续 BatchWrite 累计 worker 阻塞时长 / 总时长 | ≤ 2.5% |

如果 KPI 红：

- 输出分片优化预研报告（不在本卡实现）
- 触发 M2 警告评估是否进 P5 灰度

## 5. 测试用例

见 §3。

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R3 BatchWrite 期间 TPS 损失 > 2.5% | KPI 阈值 |

## 7. 回滚条件

p99 > 300ms 持续 → 启动分片优化分支（额外 4-6 周工时）或接受降级 TPS。

## 8. 审核 checklist

- [ ] 真主网量级 bench
- [ ] 累计阻塞 / 总时长 ≤ 2.5%
- [ ] 预研报告（如需）

## 9. 依赖 / 阻塞

- **前置**：P4.1
- **阻塞**：M2
