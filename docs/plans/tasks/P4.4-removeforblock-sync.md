# P4.4 RemoveForBlock 跟 worker commit 同步

**工时**：1 周  **owner**：TBD  **前置**：P4.1 P3.3  **阻塞**：M2

## 1. 目标

`RemoveForBlockNL` 在 ConnectBlock 内 smtx unique 持锁期间执行，跟 worker commit 完全互斥；验证 RemoveForBlock 期间 worker 阶段 4 全部阻塞 → 解锁后看到新 mempool 状态 → doubleCheck 救回。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/test/removeforblock_worker_sync_tests.cpp | 新增 |

## 3. 代码改造

无新功能；只验证 P4.1 锁次序在 RemoveForBlock 路径的具体行为。

```cpp
TEST(RemoveForBlock, BlockWorkerCommitDuringExecution) {
    // worker 阶段 4 试 unique_lock(smtx) 期间触发 ConnectBlock
    // 期望：ConnectBlock 拿 smtx 先做完 RemoveForBlock + view.Flush + UpdateTip
    // worker 后拿 smtx，doubleCheck 看到 tip 变 → Resubmit
}
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| 1000 次 RemoveForBlock + 32 worker commit 注入 0 race | TSan |
| RemoveForBlock 期间 worker 全部阶段 4 → Resubmit | 单元 |

## 5. 测试用例

见 §3。

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R3 RemoveForBlock vs worker commit race | unique_lock 串行 + TSan |

## 7. 回滚条件

测试红 → 排查 P4.1 锁次序。

## 8. 审核 checklist

- [ ] 1000 次注入测试
- [ ] code review 1 cpp

## 9. 依赖 / 阻塞

- **前置**：P4.1 P3.3
- **阻塞**：M2
