# P2.1 ChainDispatcher 数据结构 + 16-shard inflight 状态机

**工时**：2 周  **owner**：TBD  **前置**：M0  **阻塞**：P2.2-P2.8 / P3.* / P4.*

## 1. 目标

实现 `ChainDispatcher` 类骨架 + 16-shard `InflightEntry` 状态机（QUEUED/RUNNING/COMMITTED/ABORTED）+ 5ms 延迟 GC（详细在 P2.6）。完整代码见 detailed-design §1.4 / §2.3。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/validation/chain_dispatcher.h | 新增 |
| src/validation/chain_dispatcher.cpp | 新增 |
| src/validation/CMakeLists.txt | 加入 |
| src/test/chain_dispatcher_skeleton_tests.cpp | 新增 |

## 3. 代码改造

```cpp
// src/validation/chain_dispatcher.h
enum class InflightStatus { QUEUED, RUNNING, COMMITTED, ABORTED };

constexpr size_t INFLIGHT_SHARDS = 16;

struct InflightEntry {
    WorkerId worker;
    InflightStatus status;
    int64_t commit_time_us = 0;
};

class ChainDispatcher {
    struct Shard {
        mutable std::shared_mutex mtx;   // LEVEL_INFLIGHT_SHARD
        std::unordered_map<TxId, InflightEntry> map;
    };
    std::array<Shard, INFLIGHT_SHARDS> inflight;
    std::vector<std::unique_ptr<PerChainWorker>> workers;
    // ... 其他成员见 detailed-design §1.4
public:
    void MarkCommitted(TxId);
    void MarkAborted(TxId);
    // 其他 API 在 P2.2-P2.7 实现
};
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| 16-shard 状态机 ops 吞吐 | > 1000 QPS（M1 决策门要求）|
| TSan 24h | 0 race |
| 状态翻转单元测试覆盖 | 4 状态 × 转换矩阵全覆盖 |

## 5. 测试用例

```cpp
TEST(ChainDispatcher, StateTransitionsValid) { /* QUEUED→RUNNING→COMMITTED */ }
TEST(ChainDispatcher, ShardingHashUniform) { /* 16-shard 分布均匀 */ }
TEST(ChainDispatcher, ConcurrentMarkCommittedNoRace) { /* 32 worker 并发 mark */ }
```

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R3 inflight race | 16-shard shared_mutex + 状态机 + TSan |

## 7. 回滚条件

QPS < 1000 或 TSan race → 重新设计 sharding 数。

## 8. 审核 checklist

- [ ] 状态机图 review
- [ ] 16-shard 分布测试
- [ ] code review 1 cpp + 1 架构

## 9. 依赖 / 阻塞

- **前置**：M0
- **阻塞**：P2.2-P2.8 / P3.* / P4.*
