# P2.3 PerChainWorker thread loop + queue + cv

**工时**：2 周  **owner**：TBD  **前置**：P2.1  **阻塞**：P2.4 / P3.*

## 1. 目标

实现 `PerChainWorker` 类：每 worker 独立线程 + queue + condvar。Push/Pop 原子，watchdog 软停语义。完整代码 detailed-design §1.5 / §3.1。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/validation/per_chain_worker.h | 新增 |
| src/validation/per_chain_worker.cpp | 新增 |
| src/test/per_chain_worker_tests.cpp | 新增 |

## 3. 代码改造

完整 stub 见 detailed-design §1.5；ProcessItem 在 P3.1-P3.3 完整。

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| Push/Pop 单线程吞吐 | > 100k ops/s |
| Push/Pop 多线程吞吐 | > 50k ops/s（带锁） |
| watchdog last_progress_us 跟踪 | 单元测试覆盖 |
| TSan 24h | 0 race |

## 5. 测试用例

```cpp
TEST(PerChainWorker, PushPopOrder) { /* FIFO */ }
TEST(PerChainWorker, ConcurrentPushPop) { /* 32 producer × 1 worker */ }
TEST(PerChainWorker, WatchdogProgress) { /* last_progress_us 单调更新 */ }
```

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R3 worker queue race | mutex + cv，TSan |

## 7. 回滚条件

吞吐不达标 → 改 lock-free queue 或调队列结构。

## 8. 审核 checklist

- [ ] FIFO + watchdog 单元覆盖
- [ ] TSan 24h 0 race
- [ ] code review 1 cpp

## 9. 依赖 / 阻塞

- **前置**：P2.1
- **阻塞**：P2.4 / P3.*
