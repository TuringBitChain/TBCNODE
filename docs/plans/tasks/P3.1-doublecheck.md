# P3.1 doubleCheck 4 项实现 + H-D perInputScriptFlags

**工时**：1-2 周  **owner**：TBD  **前置**：P0.3 P1.1 P2.1  **阻塞**：P3.3 / P5.6

## 1. 目标

实现 worker ProcessItem 阶段 3 + 阶段 4 的 4 项 doubleCheck 协议（C1 修复：全部用 snap2 比对，不在 smtx 内调 chainstate）+ H-D 修复 perInputScriptFlags 用 GetInputScriptBlockHeight 等价复刻（含 MEMPOOL_HEIGHT 哨兵分支处理）。完整代码 detailed-design §3.2 + §6。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/validation/per_chain_worker.cpp | ProcessItem 阶段 3 + 4 |
| src/validation.cpp | 给 worker 调用的 DoVerifyScripts 重载（加 per_input_flags 参数）|
| src/test/doublecheck_protocol_tests.cpp | 新增 |
| src/test/perinput_scriptflags_equiv_tests.cpp | 新增 |

## 3. 代码改造

完整 ProcessItem 代码 detailed-design §3.2（含 H-D + P-1 修复后的 perInputScriptFlags 推导）。

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| H-D 算法等价性测试矩阵 5 项 | 全过（chain UTXO < / >= genesisActivationHeight 各 + MEMPOOL_HEIGHT < / >= 各 + regtest 边缘 case）|
| 4 项 doubleCheck 单元覆盖 | 100% |
| race 注入测试（tip 变 / script_flags 变 / mempool 父 evict / chain UTXO 双花）| 全过 |
| TSan 24h | 0 race |

## 5. 测试用例

```cpp
// H-D
TEST(PerInputFlags, ChainCoinPreGenesis) { /* < genesisActivationHeight → 0 */ }
TEST(PerInputFlags, ChainCoinPostGenesis) { /* >= → SCRIPT_UTXO_AFTER_GENESIS */ }
TEST(PerInputFlags, MempoolHeightPreGenesisChain) { /* snap.height+1 < genesis */ }
TEST(PerInputFlags, MempoolHeightPostGenesisChain) { /* snap.height+1 >= genesis */ }
TEST(PerInputFlags, RegtestEdgeCase) { /* */ }

// doubleCheck
TEST(DoubleCheck, TipChangedResubmit) { /* #1 */ }
TEST(DoubleCheck, ScriptFlagsChangedResubmit) { /* #2 */ }
TEST(DoubleCheck, DoubleSpendDetected) { /* #3a */ }
TEST(DoubleCheck, ChainCoinAlreadySpentReadAgain) { /* #3b H7 */ }
TEST(DoubleCheck, MempoolParentEvicted) { /* #4 */ }
```

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R3 doubleCheck race 漏 | 4 项 + H-D 共识等价性测试 |
| R6 perInputScriptFlags 非等价 → 共识漂移 | 5 项等价矩阵 + P6.4 shadow node |

## 7. 回滚条件

H-D 等价性测试失败 → P-1 算法重写；doubleCheck 漏 → 补单元 + race 注入。

## 8. 审核 checklist

- [ ] H-D 5 项等价性证据
- [ ] 4 项 doubleCheck race 注入测试
- [ ] code review 1 共识 + 1 cpp + 1 安全

## 9. 依赖 / 阻塞

- **前置**：P0.3 P1.1 P2.1
- **阻塞**：P3.3 / P5.6
