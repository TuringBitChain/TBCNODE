# P0.6 单元测试 + TSan/helgrind 72 小时压测

**工时**：2-3 周  **owner**：TBD  **前置**：P0.1-P0.5  **阻塞**：P0.7 / M0

## 1. 目标

P0 全部改动合并后跑 TSan + helgrind 72h 长压；新增 race 数 = 0（vs P0.0a.2 baseline）。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| .github/workflows/p0-72h-soak.yml | 新增 72h CI |
| docs/plans/spike-results/P0-soak-report.md | 报告归档 |

## 3. 代码改造

无新代码，只跑测试 + 收集 log。

```bash
# 72h soak
cmake -B build-tsan -Denable_tsan=ON
cmake --build build-tsan -j$(nproc)
./tools/sanitizer-72h-soak.sh build-tsan

# 重点测试集（P0 改动相关）
ctest --test-dir build-tsan -L unit -R "coins|libcuckoo|sync"
test/functional/test_runner.py --extended  # 循环 24h × 3
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| TSan 72h race 数 vs baseline | 0 新增 |
| helgrind 72h race vs baseline | 0 新增 |
| ASan 72h leak | 0 新增 |
| 单元 coins_* 全过 | 100% |
| functional --extended 循环 24h × 3 | 0 失败 |

## 5. 测试用例

见 §3；72h 自动化 CI 完整跑一次。

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R3 P0 综合并发 race | 72h 长压 + helgrind 双重 |

## 7. 回滚条件

新增 race ≥ 1 → 排查到具体 P0.X 卡 → revert 该卡 → 重新设计。

## 8. 审核 checklist

- [ ] 72h log 全文归档
- [ ] race / leak 分类（known / new / suppression）
- [ ] code review by 1 安全 + 1 cpp

## 9. 依赖 / 阻塞

- **前置**：P0.1-P0.5
- **阻塞**：P0.7 / M0
