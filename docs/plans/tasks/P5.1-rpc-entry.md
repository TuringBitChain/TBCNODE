# P5.1 sendrawtransaction / sendrawtransactions 改 dispatcher（保留逐笔语义）

**工时**：1 周  **owner**：TBD  **前置**：P3.5 P3.3  **阻塞**：P5.3

## 1. 目标

`src/rpc/rawtransaction.cpp` 改造：
- `sendrawtransaction` 单笔走 `GetDispatcher().SubmitSync(tx)`
- `sendrawtransactions` 老批 RPC **保留逐笔语义**（F7）：`for tx in batch: SubmitSync(tx)`，部分成功不变
- 不破坏现有 RPC 客户端

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/rpc/rawtransaction.cpp | sendrawtransaction / sendrawtransactions 改造 |
| src/test/rpc_compatibility_tests.cpp | 新增（v1 vs v2.6.1 行为对比）|
| test/functional/rpc_legacy_compat.py | 新增 |

## 3. 代码改造

完整 detailed-design §8.1 + §8.2（v2.6 修订后版本）。

```cpp
UniValue sendrawtransaction(const Config& config, const JSONRPCRequest& request) {
    TxRef tx = DecodeHexTx(request.params[0].get_str());
    CValidationState state = GetDispatcher().SubmitSync(tx);
    if (state.IsValid()) return tx->GetId().ToString();
    if (state.GetRejectCode() == REJECT_OVERLOADED) {
        throw JSONRPCError(RPC_VERIFY_REJECTED, "server-busy: " + state.GetRejectReason());
    }
    throw JSONRPCError(RPC_TRANSACTION_REJECTED, FormatStateMessage(state));
}

UniValue sendrawtransactions(const Config& config, const JSONRPCRequest& request) {
    auto txs = DecodeHexTxArray(request.params[0]);
    std::vector<CValidationState> results;
    for (auto& tx : txs) results.push_back(GetDispatcher().SubmitSync(tx));
    return BuildBatchResult(results);   // 跟现状格式 100% 一致
}
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| sendrawtransaction 行为对比 v1 0 偏差 | functional |
| sendrawtransactions 部分成功语义保留 | functional |
| REJECT_OVERLOADED 路径正确 | functional |
| 跨语言 RPC 客户端兼容（C++/Python/Java/Go SDK）| 至少 4 SDK 测过 |

## 5. 测试用例

```python
# test/functional/rpc_legacy_compat.py
class RpcLegacyCompatTest(...):
    def run_test(self):
        # 跑 v1 binary 输出 + v2.6.1 binary 输出
        # 同样 input → 输出 100% 一致
```

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R6 RPC 行为偏差破坏生态 | 跨 SDK 兼容性测试 |
| R3 dispatcher 路径并发 | P0-P4 已覆盖 |

## 7. 回滚条件

任一 SDK 不兼容 → revert RPC 改造，回原 PTV 路径。

## 8. 审核 checklist

- [ ] 4 个 SDK 兼容性测过
- [ ] 跨 v1/v2.6 输出 0 偏差
- [ ] code review 1 协议 + 1 cpp

## 9. 依赖 / 阻塞

- **前置**：P3.5 P3.3
- **阻塞**：P5.3
