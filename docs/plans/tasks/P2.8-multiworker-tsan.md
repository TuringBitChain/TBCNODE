# P2.8 单元测试 + 多 worker 并发压测

**工时**：2-3 周  **owner**：TBD  **前置**：P2.1-P2.7  **阻塞**：M1

## 1. 目标

P2 全部改动合并后跑 32 worker × 1h 持续压测，验证 ChainDispatcher + Worker pool 综合行为；TSan + helgrind 72h 0 race。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/test/dispatcher_stress_tests.cpp | 新增 |
| docs/plans/spike-results/P2-stress-report.md | 报告 |

## 3. 代码改造

```cpp
TEST(DispatcherStress, ThirtyTwoWorker1Hour) {
    // 32 worker，模拟 1000 RPC 客户端，
    // 混合：独立链 60% + 子孙链 30% + reorg 10%
    // 1h 后断言：吞吐 / inflight 大小 / stash 大小 / metrics 全合理
}
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| 32 worker 1h 压测吞吐 | > 600 TPS |
| dispatcher QPS | > 1000（M1 决策门）|
| TSan 72h | 0 race |
| helgrind 24h | 0 race |
| inflight 表大小 | < 100k 稳态 |
| stash 占用 | < 50% MAX_SIZE |

## 5. 测试用例

见 §3。

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R3 综合并发 race | 72h 长压 |

## 7. 回滚条件

任一 KPI 红 → 排查具体 P2.X 卡 → revert。

## 8. 审核 checklist

- [ ] 1h 压测报告
- [ ] TSan/helgrind log
- [ ] code review 1 架构 + 1 cpp

## 9. 依赖 / 阻塞

- **前置**：P2.1-P2.7
- **阻塞**：M1
