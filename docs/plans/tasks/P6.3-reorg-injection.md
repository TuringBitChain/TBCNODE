# P6.3 regtest reorg 注入测试 + reorg 风暴

**工时**：1-2 周  **owner**：TBD  **前置**：P6.1 P4.2  **阻塞**：P6.7

## 1. 目标

regtest 上注入 reorg 各类极端场景 + reorg 风暴（6 块 + 30 万 tx 涌入 + ReorgStash 满载）。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| test/functional/reorg_injection_extreme.py | 新增 |
| test/functional/reorg_storm_30w_tx.py | 新增 |

## 3. 代码改造

```python
# reorg_storm_30w_tx.py
class ReorgStorm30WTxTest(...):
    def run_test(self):
        # 30 万 tx 入 mempool
        # 触发 6 块 reorg
        # 验证：mempool 最终态跟 v1 一致
        #       ReorgStash 落 stash 0 丢失
        #       TPS ≥ 200 维持
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| 6 块 reorg + 30 万 tx 涌入 mempool 最终态跟 v1 一致 | 通过 |
| TPS 维持 ≥ 200 | bench |
| 0 panic / 0 crash | 集成 |
| ReorgStash metrics 合理（push_total / drop / drain） | 单元 |

## 5. 测试用例

见 §3。

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R5 reorg 风暴 | 30 万 tx 极端测试 |

## 7. 回滚条件

行为偏差 → 排查 P2.4 ReorgStash 设计 → revert。

## 8. 审核 checklist

- [ ] 30 万 tx 极端测试通过
- [ ] code review 1 共识 + 1 架构

## 9. 依赖 / 阻塞

- **前置**：P6.1 P4.2
- **阻塞**：P6.7
