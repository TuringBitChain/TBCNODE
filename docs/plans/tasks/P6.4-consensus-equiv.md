# P6.4 共识等价性测试（v2.6.1 vs origin/main UTXO hash + getrawmempool diff）

**工时**：2 周  **owner**：TBD  **前置**：P0.0b.3 P6.1  **阻塞**：P6.7

## 1. 目标

跑 v2.6.1 在 P0.0b.3 baseline 全部采样高度上的 UTXO hash + getrawmempool；对比 100% 一致。这是 R4 风险（共识等价性 30% 覆盖率）的最终验证。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| tools/consensus-equivalence-check.py | 新增 |
| docs/plans/spike-results/P6.4-equivalence-report.md | 报告 |

## 3. 代码改造

```python
# tools/consensus-equivalence-check.py
def main():
    baseline = load_json("consensus-baseline-v2.6.1.json")
    for height, expected in baseline.items():
        v26_result = run_v26_at_height(height)
        if v26_result['utxo_hash'] != expected['utxo_hash']:
            print(f"DIVERGE @ {height}: {v26_result} vs {expected}")
            sys.exit(1)
        if v26_result['mempool_diff_count'] > 0:
            # 注意：mempool 在 reindex 时为空，diff 应为 0
            # 实际比对在 P6.6 shadow node 阶段
            pass
    print("OK: 100% UTXO hash 一致")
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| baseline 全采样高度 UTXO hash 一致 | 100% |
| 关键激活高度 ±1000 块 0 diff | 100% |
| mempool diff（重新跑同样 mempool 数据）| 0 持久 diff |

## 5. 测试用例

```bash
python tools/consensus-equivalence-check.py \
    --baseline /data/baseline/consensus-baseline-v2.6.1.json \
    --vn-binary $PWD/build/src/bitcoind \
    --datadir /data/v26-replay
```

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R4 共识等价性覆盖率不够 | 30%+ 采样 + 关键激活全量 |
| R6 替换漏一处导致共识漂移 | 100% 一致才进 P6.7 |

## 7. 回滚条件

任一高度 diff → 排查具体改造点 → revert 或修复 → 重跑 P6.4。

## 8. 审核 checklist

- [ ] 100% 一致证据
- [ ] code review 1 共识 + 1 安全

## 9. 依赖 / 阻塞

- **前置**：P0.0b.3 P6.1
- **阻塞**：P6.7
