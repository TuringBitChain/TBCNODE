# P5.3 废除 processValidation / mNewTxnsThread / ValidationScheduler

**工时**：1-2 周  **owner**：TBD  **前置**：P5.1 P5.2  **阻塞**：P5.4 P5.6

## 1. 目标

完整删除老 PTV 框架（详 detailed-design §8.5 列表）：

- `src/txn_validator.cpp` 内 processValidation 单笔/批量两个版本
- threadNewTxnHandler / mStdTxns / mNonStdTxns / mProcessingQueue / mMainMtx / mMainCV / mTxnsProcessedCV
- `src/net/validation_scheduler.h/cpp` ValidationScheduler / TOPO_SORT 整个
- `src/net.h` ParallelTxnValidation 模板

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/txn_validator.cpp / .h | 大量删除 |
| src/net/validation_scheduler.h / .cpp | 删除 |
| src/init.cpp | 删除老 PTV 初始化 |
| src/test/txn_validator_legacy_tests.cpp | 删除 / 改写 |

## 3. 代码改造

按 detailed-design §8.5 列表逐项删除。注意保留：
- `src/orphan_txns.h/cpp`（dispatcher 集成在 P5.4）
- `src/txn_recent_rejects.h/cpp`
- `src/txn_double_spend_detector.*`
- `src/txmempool.*`
- `src/script/*`
- `src/consensus/*`

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| 老 PTV 文件删除 | grep 0 残留引用 |
| 编译通过 | CI |
| 老 ctest -L unit -R "tx_validator|validation_scheduler" 删除或重写 | 100% |
| 老 functional test 通过（PTV 已迁走）| 100% |

## 5. 测试用例

无新功能；`grep "processValidation\|mMainMtx\|ValidationScheduler" src/` 应 0 结果。

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R6 删除遗漏 → 编译错误 | grep 兜底 + CI |

## 7. 回滚条件

编译不通过 → 临时保留打 deprecated 标签 / revert。

## 8. 审核 checklist

- [ ] grep 0 残留
- [ ] 编译 + functional 全过
- [ ] code review 1 cpp + 1 架构

## 9. 依赖 / 阻塞

- **前置**：P5.1 P5.2
- **阻塞**：P5.4 P5.6
