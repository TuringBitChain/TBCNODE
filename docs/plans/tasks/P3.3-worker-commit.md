# P3.3 worker 内 commit + 真 RAII rollback（H8 ScopeGuard）

**工时**：1-2 周  **owner**：TBD  **前置**：P3.1 P3.2  **阻塞**：P5.* / P4.4

## 1. 目标

worker ProcessItem 阶段 4 的真 commit：unique_lock(mempool.smtx) + AddUncheckedNL + dsDetector.Insert + commit_succeeded 标志 + ScopeGuard rollback（H8 noexcept + 内部 try/catch）。完整 detailed-design §3.2 + §7.1。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/validation/per_chain_worker.cpp | ProcessItem 阶段 4 commit + RAII rollback |
| src/util/scope_guard.h | 新增（H8）|
| src/test/worker_commit_rollback_tests.cpp | 新增 |

## 3. 代码改造

完整 detailed-design §3.2 + §7.1（H8 ScopeGuard noexcept + 内部 try/catch + commit_succeeded 标志）。

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| commit 路径 + ScopeGuard rollback 单元覆盖 | 100% |
| 异常路径 ASan 0 leak | 测试 |
| ScopeGuard 析构再抛异常被吞 | death test 不触发 |
| TSan 24h | 0 race |

## 5. 测试用例

```cpp
TEST(WorkerCommit, NormalPathCommitsAndMarks) { /* */ }
TEST(WorkerCommit, ExceptionInAddUncheckedRollsBack) { /* RAII */ }
TEST(ScopeGuard, DestructorThrowsSwallowed) { /* H8 */ }
```

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R3 commit race | unique_lock + doubleCheck（P3.1）|
| R7 异常路径锁泄漏 | ScopeGuard noexcept + ASan |

## 7. 回滚条件

异常路径 leak / commit race → 重写。

## 8. 审核 checklist

- [ ] 3 项单元 + ASan 通过
- [ ] code review 1 cpp + 1 安全

## 9. 依赖 / 阻塞

- **前置**：P3.1 P3.2
- **阻塞**：P5.* / P4.4
