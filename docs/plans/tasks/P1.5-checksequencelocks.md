# P1.5 CheckSequenceLocks Genesis 后 return true 简化

**工时**：0.5 周  **owner**：TBD  **前置**：P1.4  **阻塞**：P1.6

## 1. 目标

`src/validation.cpp:413-492` 的 `CheckSequenceLocks` 改造：TBC Genesis 后直接 return true（v2.4 已声明 BIP68 禁用）；保留 v1 路径给 regtest 兼容。

## 2. 影响文件

| 文件 | 范围 |
|------|------|
| src/validation.cpp | CheckSequenceLocks 函数体 |
| src/test/checksequencelocks_genesis_tests.cpp | 新增 |

## 3. 代码改造

```cpp
bool CheckSequenceLocks(const CTransaction& tx, int flags,
                        const Chainstate::Snapshot& snap, ...) {
    // P1.5: TBC Genesis 后等价 return true
    if (snap.isGenesisEnabled) {
        return true;
    }
    // 保留原 BIP68 路径给 pre-Genesis（regtest）
    // ... 现有实现
}
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| Genesis 后路径 return true | unit |
| pre-Genesis 路径行为不变 | regtest 测试 |
| functional test 100% 通过 | CI |

## 5. 测试用例

```cpp
TEST(CheckSequenceLocks, GenesisEnabledAlwaysTrue) { /* snap.isGenesisEnabled=true */ }
TEST(CheckSequenceLocks, PreGenesisRegtestUnchanged) { /* snap.isGenesisEnabled=false */ }
```

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R6 BIP68 退化破坏 regtest | pre-Genesis 路径完整保留 |

## 7. 回滚条件

regtest 失败 → 回到完整 BIP68 检查。

## 8. 审核 checklist

- [ ] Genesis / pre-Genesis 双路径覆盖
- [ ] code review 1 共识

## 9. 依赖 / 阻塞

- **前置**：P1.4
- **阻塞**：P1.6
