# P6.8 真主网部署 + 1 周观察（开发网通过即直接全网部署）

**工时**：1 周
**owner**：TBD
**前置**：P6.7（开发网 4 周稳定）
**阻塞**：M3（项目结束）

---

## 1. 目标

业务方澄清：TBC 开发网跟主网共识规则 100% 一样（仅 chainparams 字段不同），所以**开发网 4 周稳定通过 = 真主网兼容性已验证**。本卡直接做真主网全网部署，不再做 shadow node / canary / 渐进 10/30/100% 灰度。

部署后 1 周观察期：监控同 P6.7 KPI；任一红立即回滚 v1。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| tools/mainnet-deploy.sh | 部署脚本 |
| tools/mainnet-monitor-dashboard.json | Grafana |
| docs/plans/spike-results/P6.8-mainnet-week1-report.md | 1 周日报 |
| docs/plans/spike-results/P6.8-rollback-readiness.md | 回滚就绪报告 |

## 3. 代码改造

无新代码；部署 + 1 周监控 + 回滚就绪。

```bash
# tools/mainnet-deploy.sh
# 1. 在所有真主网节点部署 v2.6.1 binary（保留 v1 binary 在系统上）
# 2. 同步切换（不分批，因为开发网已 4 周验证过 = 跟真主网行为一致）
# 3. 监控启动后 1 周

# tools/mainnet-rollback.sh（保留待用）
# 失败时一行命令切回 v1
systemctl stop bitcoind-v26 && \
ln -sf /usr/local/bin/bitcoind-v1 /usr/local/bin/bitcoind && \
systemctl start bitcoind
```

## 4. 验收 KPI

| KPI | 类型 | 目标 |
|-----|------|------|
| 1 周连续运行 0 panic / 0 fork | 硬 | 通过 |
| chain tip 跟其他节点 100% 一致 | 硬 | 100% |
| TPS 实测（真主网负载） | 软 | ≥ 600 |
| p99 重要 RPC 延迟 | 软 | < 100ms |
| 重要 RPC 客户端（钱包 / 交易所 / 矿池）0 异常报告 | 硬 | 通过 |
| mempool diff 监控 1 周 0 持久阈值告警 | 硬 | 通过 |
| 回滚演练（dry-run）成功 | 硬 | 通过（Day 1 必做）|

## 5. 测试用例

```bash
# Day 1 强制回滚演练（在备节点上做）
./tools/mainnet-rollback.sh
# 验证：v1 binary 启动 < 30s + tip 一致 + 节点继续工作

# 1 周持续监控
watch -n 60 'bitcoin-cli getbestblockhash; bitcoin-cli getmempoolinfo'
```

## 6. 风险登记

| R | 缓解 | 证据 |
|---|------|------|
| R1-R7 全部 | 开发网 4 周已充分验证 + 1 周观察 + 任一红即回滚 v1 | P6.7 报告 |
| R1 长周期人员流失 | 双人 owner + ARCHITECTURE-NOTE-P6.md | README §R1 |

## 7. 回滚条件

任一 KPI 红 → 立即执行 `tools/mainnet-rollback.sh`（< 5 分钟全网回滚）。回滚后业务继续工作（datadir 兼容），post-mortem 排查后再走 P6.7 重新验证。

## 8. 审核 checklist

- [ ] Day 1 回滚演练成功（不是 dry-run，真在备节点上做）
- [ ] 1 周观察期 0 panic / 0 fork
- [ ] 重要客户端兼容性确认（钱包 / 交易所 / 矿池）
- [ ] mempool diff 1 周 0 告警
- [ ] code review 1 运维 + 1 共识 + 业务方
- [ ] ARCHITECTURE-NOTE-P6.md hand-off 文档

## 9. 依赖 / 阻塞

- **前置**：P6.7
- **阻塞**：M3
