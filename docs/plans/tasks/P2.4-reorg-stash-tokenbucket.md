# P2.4 ReorgStash + RaceStash + token bucket + ReorgWorkerLoop

**工时**：1-2 周  **owner**：TBD  **前置**：P2.1 / P2.3  **阻塞**：P2.5 / P3.2

## 1. 目标

实现 `TxStash` 模板（H-E + N7 + F6）：`ReorgStash`（200k / 10min TTL）+ `RaceStash`（100k / 5min TTL）。+ `ResubmitRateLimiter` token bucket（1000/s）+ `ReorgWorkerLoop`（独立队列，按断链深度动态扩上限 10000-100000）。完整代码 detailed-design §1.4 + §2.5。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/validation/tx_stash.h | 新增（模板）|
| src/validation/chain_dispatcher.cpp | TriggerReorgResubmit + ReorgWorkerLoop + ReorgStashDrain + Resubmit 集成 RaceStash |
| src/test/tx_stash_tests.cpp | 新增 |
| src/test/reorg_stash_drain_tests.cpp | 新增 |

## 3. 代码改造

完整代码 detailed-design §1.4 + §2.5（TxStash 模板 + Drain 消费式 + GC + metrics）。

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| ReorgStash Push/Drain/GC 单线程性能 | > 50k ops/s |
| 100 线程并发 Push/Drain 0 race | TSan 24h |
| GC TTL 准确性 | < 1% 偏差 |
| 6 块 reorg + 30 万 tx 涌入：0 tx 丢失（落 stash 不丢）| 集成测试 |
| token bucket 限速 1000/s | 误差 < 5% |
| metrics（push_total/drop_full/drop_ttl/drain_total）准确 | 单元 |

## 5. 测试用例

```cpp
TEST(TxStash, PushDrainGCConcurrent) { /* 100 线程 */ }
TEST(TxStash, TTLAccuracy) { /* GC 时间窗口 */ }
TEST(TxStash, OverflowDropsOldestNotNewest) { /* H-E FIFO */ }
TEST(ReorgWorkerLoop, DrainAfterQueueEmpty) { /* 回灌 */ }
TEST(TokenBucket, RateLimitWithinTolerance) { /* 1000/s ±5% */ }
```

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R5 reorg 风暴 30 万 tx | dynamic cap + ReorgStash 200k + ReorgStashDrain 回灌 |
| R3 stash race | 单 mtx 串行 + TSan |
| R7 RaceStash 不污染 orphan_txns | metrics 分开 |

## 7. 回滚条件

任一 KPI 红 → 回到 v2.4 的 orphan_txns 兜底（污染但能跑），M1 警告。

## 8. 审核 checklist

- [ ] 100 线程 race 测试通过
- [ ] 6 块 reorg 集成测试 0 丢失
- [ ] metrics 准确
- [ ] code review 1 架构 + 1 cpp

## 9. 依赖 / 阻塞

- **前置**：P2.1 / P2.3
- **阻塞**：P2.5 / P3.2
