# P4.2 worker 协调测试（reorg 期间 worker 行为）

**工时**：2-3 周  **owner**：TBD  **前置**：P4.1 P2.4  **阻塞**：P4.4

## 1. 目标

集成测试：reorg 期间 worker 行为正确——worker 阶段 1 拍 snap、阶段 2 等 batchWriteMtx、阶段 3 验证、阶段 4 doubleCheck #1 触发 Resubmit、ReorgWorkerLoop 处理 disconnected tx 重新派发。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/test/worker_reorg_coord_tests.cpp | 新增 |
| test/functional/reorg_worker_coord.py | 新增 |

## 3. 代码改造

```cpp
TEST(WorkerReorgCoord, WorkerSnapTipChangeResubmits) {
    // 32 worker 在跑，触发 ConnectBlock + UpdateTip
    // 期望：所有 in-flight worker 阶段 4 doubleCheck #1 触发 Resubmit
    // 共识漂移 = 0
}

TEST(WorkerReorgCoord, ReorgDisconnectedTxRoutedThroughReorgQueue) {
    // 6 块 reorg → DisconnectTip → RemoveForReorg → TriggerReorgResubmit
    // 期望：tx 走 reorg_queue 不消耗 token bucket
}
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| 100 次 reorg 注入 0 共识漂移 | 集成 |
| reorg 期间 worker 阶段 4 全部 Resubmit | 单元 |
| reorg_queue 不消耗 token bucket | metrics |
| TSan 24h | 0 race |

## 5. 测试用例

见 §3。

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R5 reorg 期间 worker 行为不定 | 100 次注入 + functional |

## 7. 回滚条件

共识漂移 → 回 P4.1 排查。

## 8. 审核 checklist

- [ ] 100 次 reorg 注入通过
- [ ] code review 1 共识 + 1 架构

## 9. 依赖 / 阻塞

- **前置**：P4.1 P2.4
- **阻塞**：P4.4
