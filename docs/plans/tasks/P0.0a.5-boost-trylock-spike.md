# P0.0a.5 boost::recursive_mutex try_lock 4 版本 spike + H-F 异常注入

**工时**：1 周
**owner**：TBD
**前置**：无
**阻塞**：P1.1（Chainstate seqlock UpdateTip 单写者守卫依赖此 spike 通过）/ M-1a 决策门

---

## 1. 目标

验证 F2 单写者运行时守卫的核心假设：`boost::recursive_mutex::try_lock` 在「同线程已持锁」返回 true 并递增 count、在「跨线程」返回 false。该假设在 4 个 boost 版本（1.65/1.71/1.74/1.83）上必须全过；任一版本不符 → 切换备选方案 `std::atomic<std::thread::id> writer_owner`。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/test/recursive_mutex_spike_tests.cpp | 新增 |
| .github/workflows/boost-version-matrix.yml | 新增 4 boost 版本 CI matrix |
| docs/plans/spike-results/boost-trylock.md | 输出报告归档 |

## 3. 代码改造

### 3.1 5 项语义测试

```cpp
// src/test/recursive_mutex_spike_tests.cpp
#include <boost/thread/recursive_mutex.hpp>
#include <boost/thread/lock_guard.hpp>
#include <boost/test/unit_test.hpp>
#include <atomic>
#include <thread>
#include <chrono>

BOOST_AUTO_TEST_SUITE(recursive_mutex_spike)

// 测试 1：主线程持锁 → 主线程 try_lock → 期望 true
BOOST_AUTO_TEST_CASE(self_thread_already_holding) {
    boost::recursive_mutex m;
    boost::lock_guard<boost::recursive_mutex> lg(m);
    BOOST_REQUIRE(m.try_lock());   // 期望 true，count 递增
    m.unlock();   // 还回去
}

// 测试 2：主线程持锁 → 副线程 try_lock → 期望 false（核心断言）
BOOST_AUTO_TEST_CASE(other_thread_blocked) {
    boost::recursive_mutex m;
    std::atomic<bool> result{true};

    boost::lock_guard<boost::recursive_mutex> lg(m);
    std::thread t([&] {
        result = m.try_lock();
        if (result) m.unlock();
    });
    t.join();
    BOOST_REQUIRE(!result);   // 期望 false
}

// 测试 3：递归 N 次持锁 + try_lock + unlock 平衡
BOOST_AUTO_TEST_CASE(recursive_count_balanced) {
    boost::recursive_mutex m;
    constexpr int N = 100;
    for (int i = 0; i < N; i++) m.lock();
    BOOST_REQUIRE(m.try_lock());   // 期望 true
    m.unlock();   // 还回 try_lock 那一层
    for (int i = 0; i < N; i++) m.unlock();   // 还回所有层
    // 不抛异常即 pass
}

// 测试 4：高频路径 baseline（< 10ns/次）
BOOST_AUTO_TEST_CASE(performance_baseline) {
    boost::recursive_mutex m;
    constexpr int N = 1'000'000;
    auto t0 = std::chrono::steady_clock::now();
    for (int i = 0; i < N; i++) {
        m.lock();
        if (m.try_lock()) m.unlock();
        m.unlock();
    }
    auto t1 = std::chrono::steady_clock::now();
    auto ns = std::chrono::duration_cast<std::chrono::nanoseconds>(t1 - t0).count();
    double per_op = double(ns) / N;
    BOOST_TEST_MESSAGE("per try_lock+unlock: " << per_op << " ns");
    BOOST_REQUIRE_LT(per_op, 50.0);   // 50ns 上限（10ns 是理想）
}

// 测试 5：H-F 异常注入 — RAII unique_lock 析构应平衡 count
BOOST_AUTO_TEST_CASE(exception_safety_raii_balanced) {
    boost::recursive_mutex m;
    m.lock();   // 主线程持有 1 层

    bool exception_thrown = false;
    try {
        std::unique_lock<boost::recursive_mutex> probe(m, std::try_to_lock);
        BOOST_REQUIRE(probe.owns_lock());
        throw std::runtime_error("simulated LogPrintf OOM");
    } catch (const std::runtime_error&) {
        exception_thrown = true;
    }
    BOOST_REQUIRE(exception_thrown);

    // 验证 count 平衡：主线程仍能 unlock 一次（如果 RAII 没释放，多了 1 层）
    m.unlock();
    // 如果还能 try_lock 说明 count 已归零，跨线程能拿
    std::atomic<bool> other_can_lock{false};
    std::thread t([&] {
        other_can_lock = m.try_lock();
        if (other_can_lock) m.unlock();
    });
    t.join();
    BOOST_REQUIRE(other_can_lock);
}

BOOST_AUTO_TEST_SUITE_END()
```

### 3.2 备选方案（spike 失败时切换）

```cpp
// src/validation/chainstate.h（备选实现）
#include <atomic>
#include <thread>

class Chainstate {
    std::atomic<std::thread::id> writer_owner{std::thread::id{}};
    // ...

public:
    void UpdateTip(const CBlockIndex* new_tip, uint32_t script_flags)
        EXCLUSIVE_LOCKS_REQUIRED(cs_main)
    {
        // 备选：CAS 探针
        std::thread::id self = std::this_thread::get_id();
        std::thread::id expected{};
        if (!writer_owner.compare_exchange_strong(expected, self,
                                                   std::memory_order_acquire)) {
            // 已有其它线程在写 → 跨线程冲突
            LogPrintf("FATAL: UpdateTip cross-thread call detected\n");
            std::abort();
        }
        // 临界区
        // ... seqlock writer
        writer_owner.store(std::thread::id{}, std::memory_order_release);
    }
};
```

### 3.3 boost 版本 CI matrix

```yaml
# .github/workflows/boost-version-matrix.yml
jobs:
  trylock-spike:
    strategy:
      matrix:
        boost: [1.65.0, 1.71.0, 1.74.0, 1.83.0]
        os: [ubuntu-20.04, ubuntu-22.04]
    steps:
      - name: Install boost ${{ matrix.boost }}
        run: |
          wget https://boostorg.jfrog.io/artifactory/main/release/${{ matrix.boost }}/source/boost_${{ matrix.boost }}.tar.gz
          tar xzf boost_${{ matrix.boost }}.tar.gz
          cd boost_*
          ./bootstrap.sh && ./b2 install --prefix=$HOME/boost-install
      - name: Build & test
        run: |
          cmake -B build -DBOOST_ROOT=$HOME/boost-install
          cmake --build build --target test_bitcoin
          ./build/src/test/test_bitcoin --run_test=recursive_mutex_spike/*
```

## 4. 验收 KPI

| KPI | 目标 | 测量 |
|-----|------|------|
| 测试 1 同线程 try_lock 返回 true | 4 boost 版本全过 | CI matrix |
| 测试 2 跨线程 try_lock 返回 false | 4 boost 版本全过（核心断言）| CI matrix |
| 测试 3 递归 count 平衡 | 4 版本全过 | CI matrix |
| 测试 4 性能 < 50ns/op | 4 版本全过 | bench |
| 测试 5 H-F 异常注入 RAII 平衡 | 4 版本全过 | CI matrix |
| ASan 异常路径 0 leak | helgrind / ASan log | sanitizer |

任一 KPI 不达标 → 切换备选方案（§3.2 std::atomic<thread::id>），方案需要重新 spike 同样 5 项测试 1 周。

## 5. 测试用例

见 §3.1 单元测试 5 项 + §3.3 CI matrix 4 boost × 2 OS = 8 组合。

## 6. 风险登记

| R | 缓解 | 证据 |
|---|------|------|
| R2 跨 boost 版本 | 4 版本 CI matrix | §3.3 |
| R7 异常路径 use-after-free | 测试 5 + ASan 验证 | §4 KPI |

## 7. 回滚条件

任一 KPI 不达标 → 切备选方案 `std::atomic<thread::id>`（§3.2），CCriticalSection 包装 + 5 项语义测试同样跑一遍。如备选也失败 → M-1a 失败 → 整套 v2.6.1 放弃。

## 8. 审核 checklist

- [ ] 5 项测试代码 review by 1 cpp
- [ ] CI matrix 4 boost × 2 OS 全跑通
- [ ] 报告归档 docs/plans/spike-results/boost-trylock.md
- [ ] 如果通过 → 确认 detail §1.1 F2 实现保留 try_lock 路径
- [ ] 如果失败 → §3.2 备选方案落地新卡

## 9. 依赖 / 阻塞

- **前置**：无
- **阻塞**：P1.1（Chainstate UpdateTip 守卫）/ M-1a
