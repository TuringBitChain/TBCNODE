# P6.5 retry 风暴回归测试

**工时**：1 周  **owner**：TBD  **前置**：P6.1 P3.2 P2.4  **阻塞**：P6.7

## 1. 目标

100 客户端 × 1000 笔 × 1h 持续注入，叠加 reorg / BatchWrite / worker crash 注入；验证 RaceStash + ReorgStash + token bucket 在风暴下不丢 tx 不死锁。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| test/functional/retry_storm_1h.py | 新增 |
| docs/plans/spike-results/P6.5-storm-report.md | 报告 |

## 3. 代码改造

```python
# 100 客户端并发，每秒 100 sendrawtransaction
# 同时每 5 分钟触发一次 reorg + 每 10 分钟模拟 worker crash
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| 1h 100 客户端 × 100/s 注入 0 死锁 | 通过 |
| 0 tx 永久丢失（所有 reject 有 reason）| 通过 |
| RaceStash + ReorgStash metrics 在合理范围 | 通过 |
| TPS ≥ 600 | 通过 |
| TSan 24h（同时跑） | 0 race |

## 5. 测试用例

见 §3。

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R5 综合风暴场景 | 1h 长压 + 多类注入 |

## 7. 回滚条件

死锁 / 丢 tx → 排查 → revert P3.2 P2.4。

## 8. 审核 checklist

- [ ] 1h 风暴报告
- [ ] code review 1 架构 + 1 cpp

## 9. 依赖 / 阻塞

- **前置**：P6.1 P3.2 P2.4
- **阻塞**：P6.7
