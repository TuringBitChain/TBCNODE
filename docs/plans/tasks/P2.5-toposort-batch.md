# P2.5 SubmitBatchSync TopoSort（环检测 + 重复 txid + batch-budget 30s）

**工时**：1 周  **owner**：TBD  **前置**：P2.1 / P2.2 / P2.3  **阻塞**：P5.1 P5.5

## 1. 目标

实现 `ChainDispatcher::SubmitBatchSync` 入口拓扑排序（Kahn 算法）+ 环检测 + 重复 txid 检测；C-C batch-budget 30s（不是 per-tx 30s，整批最长 30s 阻塞 HTTP）。完整代码 detailed-design §2.2。

## 2. 影响文件

| 文件 | 改动 |
|------|------|
| src/validation/chain_dispatcher.cpp | SubmitBatchSync + TopoSort |
| src/test/toposort_tests.cpp | 新增 |

## 3. 代码改造

```cpp
std::vector<TxRef> TopoSort(std::vector<TxRef> txs) {
    std::unordered_set<TxId> seen;
    for (auto& tx : txs) {
        if (!seen.insert(tx->GetId()).second) {
            throw std::invalid_argument("batch-duplicate-txid");
        }
    }
    // Kahn 算法...
    // 残留 in-degree > 0 → throw "batch-cycle"
}

std::vector<CValidationState> ChainDispatcher::SubmitBatchSync(std::vector<TxRef> txs) {
    // 详细见 detailed-design §2.2 + C-C batch-budget 30s 修复
    auto deadline = std::chrono::steady_clock::now() + std::chrono::seconds(30);
    // ...
}
```

## 4. 验收 KPI

| KPI | 目标 |
|-----|------|
| 100 笔无环 batch TopoSort 耗时 | < 10ms |
| 环检测准确性 | 100% |
| 重复 txid 检测 | 100% |
| batch-budget 30s 不阻塞 50min | 集成测试 |
| TSan 24h | 0 race |

## 5. 测试用例

```cpp
TEST(TopoSort, NoCycleSorts) { /* 父子链拓扑正确 */ }
TEST(TopoSort, CycleRejected) { /* 环抛 batch-cycle */ }
TEST(TopoSort, DuplicateTxidRejected) { /* 重复 txid 抛 */ }
TEST(SubmitBatchSync, BatchBudget30s) { /* 100 笔 + 1 笔 timeout 整批 30s */ }
```

## 6. 风险登记

| R | 缓解 |
|---|-----|
| R3 拓扑排序 race | 入口单线程 |
| R7 HTTP 阻塞 50min | batch-budget 30s |

## 7. 回滚条件

KPI 红 → 退回 v2.4 per-tx 30s（HTTP 阻塞接受，但 atomic 语义破坏）。

## 8. 审核 checklist

- [ ] 4 项单元覆盖
- [ ] code review 1 cpp + 1 架构

## 9. 依赖 / 阻塞

- **前置**：P2.1 / P2.2 / P2.3
- **阻塞**：P5.1 P5.5
